# 0.3.5 candidate and acceptance record

## Contract and provenance

The owner approved merging [PR #174](https://github.com/schillt/velacanto/pull/174)
into **alpha only** after required checks pass, publishing **0.3.5 as a GitHub
prerelease from alpha**, and using the existing Xcode Cloud workflow for internal
TestFlight. **No main promotion or public App Store submission is authorized.**
The native volume slider stays despite its known audible jump. Record accepted
exceptions separately from passing gates; this approval does not mean a merge,
publication or upload has occurred.

- Local packaging target: **0.3.5 (109)**. Xcode Cloud may assign a different build
  number; record the actual processed TestFlight build rather than assuming 109.
- Starting local feature candidate: `e58d0078ecdd71bf46aca09e20aa75d73030a57f`.
- Protected pre-volume fallback: `7ba4654bb16103dd2b7e8f060b48d6e8a67ee1c8`.
- Historical release: 0.3.0 (108); Foundation 106 remains the historical reference.
  Build 107 remains rejected.
- Maintained target: `NativeFoundation/VelacantoFoundation.xcodeproj`, shared
  `VelacantoFoundation` scheme. Minimum supported operating systems: 27.

## Local candidate verification

The following results apply to cleanup/packaging source
`a9a191f00ba1ae3f4f81ab9b74691df25038f1d2`, using regular local Xcode 27.0:

- Preflight: passed, zero failures; one warning for requested Xcode-check skip.
- Lint: passed with one inherited finding.
- Mac tests: **92 passed**.
- Mac and iOS Release builds: passed, including diagnostic-exclusion verification.
- Independent combined source/documentation review: passed with known limitations.

The final candidate has **not** received a new physical-device smoke test. Earlier
stage evidence below is retained without calling it exact-final device acceptance.
These local checks do not certify signing, distribution or Cloud artifacts.
This documentation correction changes the submitted SHA; record its hosted result
and the eventual integrated alpha SHA separately from the locally tested runtime.

## Evidence carried forward

The volume feature candidate had 24 passing focused Mac player tests and an iOS
build-for-testing result. Neither is physical audio or distribution evidence.
During the earlier volume-only observation, the existing journal gained zero API
or playback-error events; this does not measure native streaming or AirPlay traffic.

Owner-confirmed earlier checks included background queue advancement with audible
playback, audio-only Apple TV/HomePod output with the television remaining off,
and working lyric/queue presentation. Later direct volume testing exposed the
accepted volume defect (now deferred P2). Standalone diagnostic apps working correctly do not establish
that the combined app works correctly. macOS audible gain, VoiceOver, fixed-volume
routes and exhaustive long-duration reliability are not claimed complete here.

## Publication and distribution status

| Gate | Evidence/status |
| --- | --- |
| Independent review | Passed for a9a191f; accepted volume/playback/artwork limitations remain |
| Local checks | Passed for a9a191f as listed above; unsigned Release checks are not distribution validation |
| Device regression | Final artifact smoke not performed; earlier stage evidence only |
| Remaining physical scope | Mac audible gain, accessibility and full route matrix not claimed complete |
| Hosted PR | PR #174 run 36055252704 pending when this record was updated; the documentation correction requires its own current check result |
| Alpha integration | Authorized after required checks pass; resulting integrated SHA/checks pending |
| GitHub prerelease | Pending immutable tag and source mapping from accepted alpha; no main promotion |
| Xcode Cloud distribution | Pending archive/signing, App Store Connect processing and actual Cloud build identity |
| Internal TestFlight | Pending actual processed build availability to the existing tester group |

The existing **Alpha Internal Testflight** Xcode Cloud workflow was inspected:
NativeFoundation project and scheme, iOS build/archive, and the existing internal
tester-group post-action are configured. Its **Latest Beta or Release** selection
currently resolves to Xcode **27.2 beta (27B5019j)**, distinct from local Xcode 27.0.
It supports manual runs from a selected branch; automatic main triggering is not
authorization to promote main. No workflow settings were changed. Record the
selected alpha commit, actual Cloud toolchain, workflow run, signing/archive
identity and processed version/build before claiming upload or availability.

The iPhone 17 Pro remains excluded. Keep builds serial and derived data isolated.
Do not publish raw diagnostics, credentials, URLs or personal media identifiers.

## Cleanup handoff

Preserve original Velacanto/Foundation reference apps and archives, the signed
pre-volume fallback and its source/provenance, and all dirty/unintegrated work.
Inventory source ancestry, worktree status and retained artifacts before removal.
Only remove obsolete clean worktrees once their commits are integrated or safely
retained. Remove temporary diagnostic apps separately from reference apps; never
wipe app data to tidy a workspace. Keep focused tests and useful bounded DEBUG
error attribution; temporary user-facing experiments do not belong in the release.
